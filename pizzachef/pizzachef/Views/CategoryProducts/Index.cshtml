@model IEnumerable<pizzachef.Models.CategoryProduct>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CatProName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CatProName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CatProID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CatProID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CatProID })
        </td>
    </tr>
}

</table>
